import { useAuth } from './useAuth';

export const MOCK_PROJECTS = [
  new useAuth({
    id: 1,
    isLogIn: false,
  }),
  new useAuth({
    id: 2,
    isLogIn: true,
  }),
  new useAuth({
    id: 3,
    isLogIn: true,
  })
];