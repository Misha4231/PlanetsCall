using Core.User;
using Data.Models;

namespace PlanetsCall.Helper;

public class EmailSender
{
    public void SendUserConfirmationEmail(Users user, string websiteDomain)
    {
        /*HashManager hashManager = new HashManager();
        string usernameHash = */
    }
}